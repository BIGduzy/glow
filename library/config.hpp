#ifndef GLOW_CONFIG_HPP
#define GLOW_CONFIG_HPP

namespace glow {
    #define DEBUG false // Sets the lib in debug mode, when in debug some variables get printed to console (debug slows down the library)
}

#endif // CONFIG_HPP
